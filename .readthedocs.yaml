version: 2

python:
  install:
    - requirements: docs/source/rtd_requirements.txt
    - method: pip
      path: .

build:
  os: ubuntu-24.04
  tools:
    python: "3"

sphinx:
  # Path to your Sphinx configuration file.
  configuration: docs/source/conf.py
